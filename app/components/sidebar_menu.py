# app/sidebar.py

import streamlit as st


def draw_sidebar(is_admin: bool = False) -> str:
    """
    사이드바 메뉴 전체 구성
    - is_admin: True일 경우 관리자 메뉴 노출
    - 반환값: 선택된 메뉴 문자열
    """

    with st.sidebar:
        st.markdown(
            """
            <div style="font-size:22px; font-weight:800; margin-bottom:20px; background-color: #ffffff;">
                📁 메뉴
            </div>
            """,
            unsafe_allow_html=True
        )

        # ---------------------------
        # 기본 메뉴
        # ---------------------------
        menu_items = [
            "🏠 메인 대시보드",
            "📑 정산 페이지",     # ← 모든 정산 기능 버튼 방식으로 이 페이지에서 처리
            "📝 기안 자료 조회",
        ]

        # ---------------------------
        # 관리자 메뉴
        # ---------------------------
        if is_admin:
            menu_items.extend(
                [
                    "🛠 관리자 설정",
                    "📜 시스템 로그",
                    "⚙ 환경 설정",
                ]
            )

        # ---------------------------
        # 공통 메뉴
        # ---------------------------
        menu_items.append("🚪 로그아웃")

        # ---------------------------
        # 라디오 UI (사이드바 선택)
        # ---------------------------
        choice = st.radio(
            label="",
            options=menu_items,
            index=0,
        )

    # 정제된 반환값
    return choice
